import React from "react";
import { TextField } from "@mui/material";

const SidebarSearch = () => {
  return (
    <TextField fullWidth variant="outlined" label="Arama yap..." size="small" />
  );
};

export default SidebarSearch;
